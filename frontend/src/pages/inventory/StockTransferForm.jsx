import React from 'react';
import ParadoxTimer from '../../components/ParadoxTimer';

const StockTransferForm = () => {
  return (
    <ParadoxTimer
      moduleName="Stock Transfer Editor"
      description="Stock Transfer creation and edit tools are currently locked inside a temporal stabilization field. Please wait for the chronotrons to align."
    />
  );
};

export default StockTransferForm;
